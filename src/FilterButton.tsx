import {Button} from './Button';
import React from 'react';

export const FilterButton = () => {
    return (
        <div>
            <Button title="All"/>
            <Button title="Active"/>
            <Button title="Completed"/>
        </div>)
};